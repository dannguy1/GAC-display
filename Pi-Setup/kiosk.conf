# GAC Kiosk Configuration
# Edit this file then run: ~/kiosk-setup.sh apply
# To change schedule: edit times below, then run: ~/kiosk-schedule.sh apply

SERVER_IP=192.168.10.3
SHELL_PORT=8503

# Display schedule (24-hour HH:MM format)
SLEEP_TIME=00:00
WAKE_TIME=07:55
