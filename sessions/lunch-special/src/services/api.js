/**
 * Lunch-special session agent — API layer.
 *
 * Thin re-export from the shared agent SDK.
 */

export {
    insideIframe,
    setupAgent,
    reportStatus,
    getImageUrl,
} from '@gac/agent-sdk';
