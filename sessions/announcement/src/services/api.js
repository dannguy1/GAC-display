/**
 * Announcement session agent — API layer.
 *
 * Thin re-export from the shared agent SDK.
 */

export {
    insideIframe,
    setupAgent,
    reportStatus,
    onContent,
} from '@gac/agent-sdk';
